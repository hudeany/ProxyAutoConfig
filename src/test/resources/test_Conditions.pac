function FindProxyForURL(url, host){
if(3==3){
	// Always
  if (1>2) {
	    return "Never";
	  } 	else	if (host == "checkHostname"){
		return "check hostname";
		  } 	else  if (host == "otherHostname"){
		return "other hostname";
		  } else {
	return "no match";
	  }
}
}
